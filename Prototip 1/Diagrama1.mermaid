classDiagram
    class User {
        +int id
        +str username
        +str password
        +str email
        +__init__(id: int, username: str, password: str, email: str)
        +__str__() str
    }

    class users {
        +User[0..*] users
    }

    User --> users : contains
